import { StyleSheet } from "react-native";

export default StyleSheet.create({
  primaryTitle: {
    fontSize: 24,
    fontWeight: "700",
  },
  secondaryTitle: {
    fontSize: 20,
    fontWeight: "600",
    color: "#888",
  },
});

export const colors = {
  darkColor: "#222",
  darkMedium: "#444",
  primaryColor: "#e17055",
};
